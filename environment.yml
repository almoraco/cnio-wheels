name: wheels
channels:
  - conda-forge
  - defaults
dependencies:
  # Python
  - python=3.10
  - pandas
  - matplotlib

  # R
  - r-base=4.3
  - r-dplyr
  - r-ggplot2
  - r-lubridate
  - r-optparse
  - r-readr
  - r-tidyr

  # utilidades de entorno
  - pip
